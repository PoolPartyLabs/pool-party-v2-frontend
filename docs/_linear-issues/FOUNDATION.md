# FOUNDATION, Setup Issues

13 sequential issues to stand up the repo. All with label `type:setup` and priority `p0`.

---

## SETUP-001: Initialize Next.js 15 + TypeScript + Tailwind 4 repo

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`

### Body

```markdown
## Goal
Create the Next.js 15 project with strict TypeScript, Tailwind 4, App Router.

## Commands
- `pnpm create next-app@latest pool-party-frontend --typescript --tailwind --app --src-dir --import-alias "@/*" --use-pnpm`
- Confirm Node 22 LTS (`.nvmrc`).
- Add `packageManager` to `package.json`.

## Acceptance criteria
- [ ] `pnpm dev` runs at http://localhost:3000
- [ ] `tsconfig.json` has `strict: true`, `noUncheckedIndexedAccess: true`
- [ ] `tailwind.config.ts` (not .js)
- [ ] Aliases configured (`@/components/*`, `@/lib/*`, etc) per `docs/03_PROJECT_STRUCTURE.md`
- [ ] Initial folder structure created per `docs/03_PROJECT_STRUCTURE.md` (even if empty)
```

---

## SETUP-002: Configure Biome (unified lint + format)

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-001

### Body

```markdown
## Goal
Replace ESLint+Prettier with Biome.

## Tasks
- Install `@biomejs/biome`.
- Create `biome.json` with strict rules.
- Add scripts: `pnpm lint`, `pnpm format`.
- Remove the ESLint config generated by create-next-app.

## Acceptance criteria
- [ ] `pnpm lint` passes on the empty project
- [ ] `pnpm format` formats files
- [ ] Rules: no-explicit-any (error), no-unused-vars (error), prefer-const, etc
```

---

## SETUP-003: Configure Husky + commitlint + lint-staged

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-002

### Body

```markdown
## Goal
Enforce Conventional Commits and run lint/format on pre-commit.

## Tasks
- Install husky, lint-staged, commitlint.
- `npx husky init`.
- Pre-commit hook: `pnpm lint-staged`.
- Commit-msg hook: `npx commitlint --edit`.
- `commitlint.config.ts` with types: feat, fix, chore, docs, style, refactor, test, perf, i18n.
- `lint-staged.config.mjs`: `*.{ts,tsx,json}` then biome format + lint.

## Acceptance criteria
- [ ] A commit without a type prefix is rejected
- [ ] Unformatted files are formatted on commit
```

---

## SETUP-004: Configure Vitest + Testing Library + coverage

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-001

### Body

```markdown
## Goal
TDD-ready from the start.

## Tasks
- Install vitest, @testing-library/react, @testing-library/user-event, @testing-library/jest-dom, jsdom, @vitest/coverage-v8.
- `vitest.config.ts` with jsdom env, setup `tests/setup.ts`.
- Coverage thresholds per `docs/04_CODE_STANDARDS.md`.
- Scripts: `pnpm test`, `pnpm test:watch`, `pnpm test:coverage`.
- `tests/setup.ts`: registers jest-dom, global next-intl mock.
- `tests/utils/renderWithProviders.tsx`: helper injecting NextIntl + stores.

## Acceptance criteria
- [ ] `pnpm test` runs (even with no tests)
- [ ] `pnpm test:coverage` generates a report
- [ ] renderWithProviders helper ready to use
```

---

## SETUP-005: Configure next-intl (locales: en, pt-BR, es)

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-001

### Body

```markdown
## Goal
Trilingual i18n with next-intl, source in `en`, default by detection (fallback `en`).

## Tasks
- Install next-intl.
- Create `src/i18n/config.ts` with locales.
- Create `src/i18n/request.ts` (server-side loader).
- Create `src/i18n/routing.ts` (typed navigation).
- Create `src/middleware.ts` (next-intl middleware).
- Reorganize `src/app/` into `src/app/[locale]/`.
- Create `src/i18n/messages/{en,pt-BR,es}/` with a minimal `common.json`.
- Update `next.config.ts` to integrate the next-intl plugin.

## Acceptance criteria
- [ ] Accessing `/` redirects to `/<detected-locale>`
- [ ] `/pt-BR/test`, `/en/test`, `/es/test` return a separate page
- [ ] `useTranslations` works in a test client component
- [ ] `getTranslations` works in a test server component
```

---

## SETUP-006: Script `pnpm i18n:check`

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-005

### Body

```markdown
## Goal
Detect orphan keys, missing keys, and malformed ICU.

## Tasks
- Create `scripts/i18n-check.ts`.
- Detect keys used in code via AST.
- Compare with `src/i18n/messages/<locale>/*.json` files.
- Validate ICU MessageFormat.
- Fail with exit code != 0 on orphan/missing/invalid.

## Acceptance criteria
- [ ] `pnpm i18n:check` runs
- [ ] Detects a key used but not defined
- [ ] Detects a key in one language but not another
- [ ] Integrated into CI
```

---

## SETUP-007: Configure Storybook (restricted scope)

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-001

### Body

```markdown
## Goal
Storybook 8 with a NextIntl decorator, a11y addon, dark-only.

## Tasks
- Install storybook, @storybook/nextjs, addons (essentials, a11y, test).
- `npx storybook init`.
- `.storybook/main.ts`: stories from `src/components/**/*.stories.tsx` and `src/features/**/modals/*.stories.tsx`. Exclude screens.
- `.storybook/preview.tsx`: NextIntl decorator (default messages in `en`), dark background.
- Script `pnpm storybook`.

## Acceptance criteria
- [ ] `pnpm storybook` opens at http://localhost:6006
- [ ] Dark background applied
- [ ] NextIntl decorator working (test with a dummy component)
```

---

## SETUP-008: CI on GitHub Actions

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-002, SETUP-004, SETUP-006

### Body

```markdown
## Goal
Basic pipeline: lint, typecheck, test+coverage, i18n-check, build.

## Tasks
- `.github/workflows/ci.yml`.
- Parallel jobs: lint, typecheck, test, i18n, build.
- pnpm cache.
- Fail if coverage below threshold.

## Acceptance criteria
- [ ] CI runs on PR
- [ ] Blocks merge if any job fails
- [ ] Total time < 5min
```

---

## SETUP-009: Configure Claude Code (.claude/)

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`
**Depends on**: SETUP-001

### Body

```markdown
## Goal
Stand up the 9 active agents (+1 optional) and 12 skills, per `docs/06_CLAUDE_CODE_AGENTS.md`.

## Tasks
- Create `CLAUDE.md` at the root (already prepared in `docs/_claude-code-config/CLAUDE.md`).
- Create `.claude/agents/` with the agent .md files (already prepared).
- Create `.claude/skills/` with the 12 folders (already prepared) + INDEX.md.
- Configure MCP Linear, Figma, GitHub in Claude Code.

## Acceptance criteria
- [ ] `CLAUDE.md` at the root, read in the next session
- [ ] 9 active agents in `.claude/agents/`
- [ ] 12 skills in `.claude/skills/`
- [ ] MCPs authenticated and responding
```

---

## SETUP-010: Create initial `docs/IDS_REGISTRY.md`

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`

### Body

```markdown
## Goal
Central registry file ready for `figma-inventory` to populate.

## Tasks
- Create an empty file with a header and sections per area.

## Acceptance criteria
- [ ] File exists at `docs/IDS_REGISTRY.md`
- [ ] Has pre-created sections per area (CORE, DASH, POOL, POS, etc)
```

---

## SETUP-011: Create initial `docs/INTEGRATION_POINTS.md`

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`

### Body

```markdown
## Goal
Integration points inventory file, empty.

## Acceptance criteria
- [ ] File exists with a header and an empty table
```

---

## SETUP-012: Configure labels and projects in Linear

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`

### Body

```markdown
## Goal
Create all labels and projects per `docs/07_LINEAR_WORKFLOW.md`. (This can be done automatically by the `linear-bootstrap` agent.)

## Tasks
- Create projects: Foundation, Dashboard, Pools, Positions, Wallet UX, Settings, i18n, Polish.
- Create labels (full list in `docs/07_LINEAR_WORKFLOW.md` Labels section).
- Create custom status: `Needs Rules` between Triage and Ready.
- Create views: "Ready for Claude Code", "Claude Code Active", "Tech Debt", "Blocked", "Needs Rules", "i18n review".

## Acceptance criteria
- [ ] All labels created
- [ ] `Needs Rules` status active in the workflow
- [ ] Views saved
```

---

## SETUP-013: PR template and branch protection

**Project**: Foundation
**Labels**: `type:setup`, `priority:p0`

### Body

```markdown
## Goal
Enforce template usage and green CI before merge.

## Tasks
- `.github/PULL_REQUEST_TEMPLATE.md` per `docs/04_CODE_STANDARDS.md`.
- Branch protection on main: require CI passing, require PR review (placeholder, adjust later).

## Acceptance criteria
- [ ] A new PR opens with the template filled in
- [ ] Does not allow merge with red CI
```
