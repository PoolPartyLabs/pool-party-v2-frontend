/**
 * @name external links
 *
 * Shared external URLs (owned community / social links) referenced across surfaces, so the
 * canonical value lives in one place instead of being duplicated per component.
 */

/** The Pool Party community Discord invite (owned). */
export const DISCORD_INVITE_URL = "https://discord.com/invite/2Tcn6jqGRu";
