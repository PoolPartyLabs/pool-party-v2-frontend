import { PortfolioSkeleton } from "@/features/portfolio/components/PortfolioSkeleton";

/** Loading skeleton for the Portfolio screen (PP-PORT-SCR-001). */
export default function Loading() {
  return <PortfolioSkeleton />;
}
