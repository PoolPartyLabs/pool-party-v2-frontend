import { RubberRushSkeleton } from "@/features/rewards/components/RubberRushSkeleton";

/** Loading skeleton for Rubber Rush (PP-REW-SCR-001). */
export default function Loading() {
  return <RubberRushSkeleton />;
}
