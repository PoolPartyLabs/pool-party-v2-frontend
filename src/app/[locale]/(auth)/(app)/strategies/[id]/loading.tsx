import { StrategyDetailSkeleton } from "@/features/strategies/components/StrategyDetailSkeleton";

/** Loading skeleton for the Strategy detail (PP-STR-SCR-002). */
export default function Loading() {
  return <StrategyDetailSkeleton />;
}
